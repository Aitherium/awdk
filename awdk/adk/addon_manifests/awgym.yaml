# GENERATED by AitherOS/dev/tools/sync_addon_manifests_to_awdk.py -- do not edit.
# Source of truth: the same-named file in the platform's addon manifest directory.
id: awgym
brick: awgym
name: AitherGym
type: docker
default_port: 8199
image: localhost/aitheros-gym:latest
pack_id: awgym
description: An ARC training gym — a game a world model can watch, and six roles that
  play through it. Play a game end-to-end with the LeWM world model watching every
  transition, then watch the surprise score fall.
requires_plan: free
health_check:
  path: /health
  interval: 60s
http_only: false
resources:
  cpu: '2'
  memory: 4Gi
  gpu: false
endpoints:
  health: /health
  games: /gym/games
  runs: /gym/runs
  wm_health: /gym/wm/health
  score: /gym/score
federation:
  register_as: awgym
  metered_events: []
surfaces:
  ui:
    path: /
    title: awgym
    icon: gamepad-2
    size:
    - 980
    - 720
    frame: true
  commands:
  - name: play
    description: Play one ARC game with the world model watching
    run: awgym play
  - name: score
    description: Retrodiction score on unseen grids
    run: awgym score
provides_env:
  AWGYM_URL: http://127.0.0.1:{port}
