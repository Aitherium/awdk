# GENERATED by AitherOS/dev/tools/sync_addon_manifests_to_awdk.py -- do not edit.
# Source of truth: the same-named file in the platform's addon manifest directory.
id: awsh
brick: awsh
name: awsh
type: process
command: aither harness serve --host 127.0.0.1 --port {port}
default_port: 8362
description: The AitherShell harness daemon -- sessions, decisions, the terminal app's
  backend.
requires_plan: free
dependencies: []
health_check:
  path: /health
  interval: 30s
offline_mode: true
surfaces:
  commands:
  - name: sessions
    description: Live Claude Code / agent sessions
    run: awsh /sessions
provides_env:
  AITHER_HARNESS_URL: http://127.0.0.1:{port}
