# GENERATED by AitherOS/dev/tools/sync_addon_manifests_to_awdk.py -- do not edit.
# Source of truth: the same-named file in the platform's addon manifest directory.
id: awrun
brick: awrun
name: awrun
type: capability
hosted_by: awdk
default_port: 0
description: Dynamic priority for the runs you actually control -- a durable queue
  whose claim is an os.rename().
requires_plan: free
dependencies: []
health_check:
  path: /health
  interval: 30s
offline_mode: true
surfaces:
  ui:
    path: /api/local/awrun
    title: Runs
    icon: list-checks
    size:
    - 900
    - 640
    frame: true
  commands:
  - name: submit
    description: Queue a run
    run: awrun submit
  - name: queue
    description: Show the queue in priority order
    run: awrun queue
  - name: bump
    description: Move a run up
    run: awrun bump
  - name: cancel
    description: Cancel a run
    run: awrun cancel
