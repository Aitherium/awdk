# GENERATED by AitherOS/dev/tools/sync_addon_manifests_to_awdk.py -- do not edit.
# Source of truth: the same-named file in the platform's addon manifest directory.
id: awprove
brick: awprove
name: awprove — Aither World Prove
type: capability
hosted_by: awdk
default_port: 0
description: Drive a page as the real user, check what rendered, and pin it as a gate
  that goes red when it stops being true.
pack_id: awprove
requires_plan: free
dependencies: []
health_check:
  path: /health
  interval: 30s
offline_mode: true
federation:
  register_as: awprove
  metered_events:
  - awprove.run
  secrets_required: []
surfaces:
  commands:
  - name: check
    description: 'Offline: the proof is well-formed and every assertion can fail'
    run: awprove check
  - name: run
    description: Drive the journey and judge what rendered
    run: awprove run
  - name: pin
    description: Emit a self-testing gate that pins the proof
    run: awprove pin
