# GENERATED by AitherOS/dev/tools/sync_addon_manifests_to_awdk.py -- do not edit.
# Source of truth: the same-named file in the platform's addon manifest directory.
id: aitheros
brick: aitheros
name: AitherOS
type: process
hosted_by: self
default_port: 8899
description: One file you run, and the machine has a local AI stack.
requires_plan: free
dependencies: []
health_check:
  path: /health
  interval: 30s
offline_mode: true
surfaces:
  commands:
  - name: aitheros
    description: Run the local AI stack
    run: aitheros
  - name: aitheros --version
    description: One version for the whole stack
    run: aitheros --version
